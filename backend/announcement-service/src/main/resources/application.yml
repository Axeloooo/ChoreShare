server:
  port: 8085

spring:
  application:
    name: announcement-service
  config:
    import: "optional:configserver:${CONFIG_SERVICE_URI:http://localhost:8080}"
  jpa:
    database: postgresql
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:announcementdb}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:root}

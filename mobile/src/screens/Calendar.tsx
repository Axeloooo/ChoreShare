import { StyleSheet, Text, View } from "react-native";

const Calendar = () => {
  return (
    <View>
      <Text>Calendar</Text>
    </View>
  );
};

export default Calendar;

const styles = StyleSheet.create({});

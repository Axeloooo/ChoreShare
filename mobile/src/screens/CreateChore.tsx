import { StyleSheet, Text, View } from "react-native";
import React from "react";

const CreateChore = (): React.JSX.Element => {
  return (
    <View>
      <Text>CreateChore</Text>
    </View>
  );
};

export default CreateChore;

const styles = StyleSheet.create({});
